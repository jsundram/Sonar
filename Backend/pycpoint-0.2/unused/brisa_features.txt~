control_poont_av.py

    added rendering control namespace
    added rc, avt and cds service names and services

    in _new_device_event_impl(self, cargo, device_object):
        added code to cater for child devices within root devices
        added separate SOAP services for cds, avt and rc

    Added get_transport_state(self)

    Amended play(self, id) to:
        take a uri and item XML metadata - now call SetAVTransportURI
        with uri and metadata and without a prior call to browse

    Added unpause() to play the currently selected track

    Amended play() to pass InstanceID and Speed parameters
    Amended stop() to pass InstanceID parameter
    Amended pause() to pass InstanceID parameter


